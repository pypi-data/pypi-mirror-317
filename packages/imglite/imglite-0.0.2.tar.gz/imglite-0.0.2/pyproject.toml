[project]
name = "imglite"
version = "0.0.2"
description = "A SQLite-based muti-image storage specification and associated utilities."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "tqdm>=4.67.1",
]

[project.scripts]
imglite = "example:hello"
