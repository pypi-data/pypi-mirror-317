# Default virtual environment directory names for searching.
DEFAULT_ENV = [".venv", ".env", "venv"]
DEFAULT_TOML_FILE_CONFIG = {
    "project": {
        "dependencies": [],
        "optional-dependencies": {"dev": []},
    }
}
