
from .aclop_kernel import AclopKernel
import torch

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Sub(AclopKernel):
    def inner_forward(self, *xs):
        return torch.sub(*xs)

class Mul(AclopKernel):
    def inner_forward(self, *xs):
        return torch.mul(*xs)

class Div(AclopKernel):
    def inner_forward(self, *xs):
        return torch.div(*xs)

class Neg(AclopKernel):
    def inner_forward(self, *xs):
        return torch.neg(*xs)

class Pow(AclopKernel):
    def inner_forward(self, *xs):
        return torch.pow(*xs)

class Sin(AclopKernel):
    def inner_forward(self, *xs):
        return torch.sin(*xs)

class Cos(AclopKernel):
    def inner_forward(self, *xs):
        return torch.cos(*xs)

class Tan(AclopKernel):
    def inner_forward(self, *xs):
        return torch.tan(*xs)

class Exp(AclopKernel):
    def inner_forward(self, *xs):
        return torch.exp(*xs)

class Log(AclopKernel):
    def inner_forward(self, *xs):
        return torch.log(*xs)

class MatMul(AclopKernel):
    def inner_forward(self, *xs):
        return torch.matmul(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)
    
class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)

class Add(AclopKernel):
    def inner_forward(self, *xs):
        return torch.add(*xs)
