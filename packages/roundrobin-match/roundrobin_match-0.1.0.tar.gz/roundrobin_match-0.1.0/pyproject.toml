[project]
name = "roundrobin-match"
version = "0.1.0"
license = {text = "MIT License"}
description = "A CLI library for creating 1on1 and game match schedules"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
roundrobin_match = "roundrobin_match.cli:main"
