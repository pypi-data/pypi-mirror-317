[project]
name = "sqlctx"
version = "0.1.4"
description = "A tool for generating LLM context from database tables"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "duckdb",
    "pandas",
    "pyyaml",
    "psycopg2-binary",
    "mysql-connector-python",
    "fastmcp",
    "uvicorn",
    "toml>=0.10.2",
    "click>=8.1.8",
    "ruamel-yaml>=0.18.6",
]

[project.scripts]
sqlctx = "cli_commands:cli"
