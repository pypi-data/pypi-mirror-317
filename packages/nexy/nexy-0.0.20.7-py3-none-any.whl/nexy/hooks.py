
from pathlib import Path
from fastapi.responses import  HTMLResponse
# def useView(content):
#     return HTMLResponse(content=f"{content}")


def useView():
    Path(f"view/{name}/{file_path}")
    return 