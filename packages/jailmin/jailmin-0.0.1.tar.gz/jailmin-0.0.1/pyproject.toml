[build-system]
requires = [ "setuptools",]
build-backend = "setuptools.build_meta"

[project]
name = "jailmin"
version = "0.0.1"
dependencies = []

[project.scripts]
jailmin = "jailmin:doCli"

[tool.setuptools.packages.find]
where = [ "src",]
