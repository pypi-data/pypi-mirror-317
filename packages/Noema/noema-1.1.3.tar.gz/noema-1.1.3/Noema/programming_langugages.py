from .code_gen import CodeGenerator

class Python(CodeGenerator):
    hint = "Response format: Python code"

class Java(CodeGenerator):
    hint = "Response format: Java code"

class C(CodeGenerator):
    hint = "Response format: C code"

class Cpp(CodeGenerator):
    hint = "Response format: C++ code"

class CSharp(CodeGenerator):
    hint = "Response format: C# code"

class JavaScript(CodeGenerator):
    hint = "Response format: JavaScript code"

class TypeScript(CodeGenerator):
    hint = "Response format: TypeScript code"

class HTML(CodeGenerator):
    hint = "Response format: HTML code"

class CSS(CodeGenerator):
    hint = "Response format: CSS code"

class SQL(CodeGenerator):
    hint = "Response format: SQL code"

class NoSQL(CodeGenerator):
    hint = "Response format: NoSQL query"

class GraphQL(CodeGenerator):
    hint = "Response format: GraphQL query"

class Rust(CodeGenerator):
    hint = "Response format: Rust code"

class Go(CodeGenerator):
    hint = "Response format: Go code"

class Ruby(CodeGenerator):
    hint = "Response format: Ruby code"

class PHP(CodeGenerator):
    hint = "Response format: PHP code"

class Shell(CodeGenerator):
    hint = "Response format: Shell script"

class Bash(CodeGenerator):
    hint = "Response format: Bash script"

class PowerShell(CodeGenerator):
    hint = "Response format: PowerShell script"

class Perl(CodeGenerator):
    hint = "Response format: Perl code"

class Lua(CodeGenerator):
    hint = "Response format: Lua code"

class R(CodeGenerator):
    hint = "Response format: R code"

class Scala(CodeGenerator):
    hint = "Response format: Scala code"

class Kotlin(CodeGenerator):
    hint = "Response format: Kotlin code"

class Dart(CodeGenerator):
    hint = "Response format: Dart code"

class Swift(CodeGenerator):
    hint = "Response format: Swift code"

class ObjectiveC(CodeGenerator):
    hint = "Response format: Objective-C code"

class Assembly(CodeGenerator):
    hint = "Response format: Assembly code"

class VHDL(CodeGenerator):
    hint = "Response format: VHDL code"

class Verilog(CodeGenerator):
    hint = "Response format: Verilog code"

class SystemVerilog(CodeGenerator):
    hint = "Response format: SystemVerilog code"

class Julia(CodeGenerator):
    hint = "Response format: Julia code"

class MATLAB(CodeGenerator):
    hint = "Response format: MATLAB code"

class COBOL(CodeGenerator):
    hint = "Response format: COBOL code"

class Fortran(CodeGenerator):
    hint = "Response format: Fortran code"

class Ada(CodeGenerator):
    hint = "Response format: Ada code"

class Pascal(CodeGenerator):
    hint = "Response format: Pascal code"

class Lisp(CodeGenerator):
    hint = "Response format: Lisp code"

class Prolog(CodeGenerator):
    hint = "Response format: Prolog code"

class Smalltalk(CodeGenerator):
    hint = "Response format: Smalltalk code"

class APL(CodeGenerator):
    hint = "Response format: APL code"