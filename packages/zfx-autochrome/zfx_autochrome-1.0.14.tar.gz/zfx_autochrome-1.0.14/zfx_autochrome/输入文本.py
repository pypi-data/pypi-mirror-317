import time


def 输入文本(元素对象, 文本, 每次输入间隔=0.2):
    """
    向指定的元素输入文本，支持分批输入，避免输入过快导致问题。

    参数：
        - 元素对象: 已获取的网页元素对象（如输入框）。
        - 文本: 要输入的文本内容。
        - 每次输入间隔: 每个字符输入之间的间隔时间（默认为 0.2 秒）。

    返回值：
        - 成功返回 True，失败返回 False。
    """
    try:
        # 分批输入文本，每次输入一个字符，避免输入过快
        for 字符 in 文本:
            元素对象.send_keys(字符)
            time.sleep(每次输入间隔)  # 设置间隔时间
        return True  # 成功返回 True
    except Exception:
        return False  # 发生异常时返回 False