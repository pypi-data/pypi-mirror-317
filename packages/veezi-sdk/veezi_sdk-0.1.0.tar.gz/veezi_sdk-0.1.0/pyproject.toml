[project]
name = "veezi-sdk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Måns Östman", email = "mans.ostman@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.10.2",
    "requests>=2.32.3",
]

[project.scripts]
veezi-sdk = "veezi_sdk:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
