def 网页协议_响应文本_取指定行内容(响应文本, 行号):
    """
    获取响应的文本内容，并返回指定行号的文本内容。

    参数:
        - 响应文本 (str): 服务器响应的文本内容。
        - 行号 (int): 要获取的行号，从1开始计数。

    返回:
        - str: 指定行号的文本内容。如果响应为 None、指定行号超出文本行数范围、或出现任何异常，则返回空字符串。
    """
    try:
        if 响应文本 is not None:
            行列表 = 响应文本.splitlines()
            实际行号 = 行号 - 1  # 将1开始的行号转换为0开始的索引
            if 0 <= 实际行号 < len(行列表):
                指定行文本 = 行列表[实际行号]
                return 指定行文本
            else:
                return ''
        else:
            return ''
    except Exception:
        return ''