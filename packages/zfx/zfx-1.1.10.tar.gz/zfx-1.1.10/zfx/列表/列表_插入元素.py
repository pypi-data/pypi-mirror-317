def 列表_插入元素(列表, 位置, 元素):
    """
    在指定位置插入一个元素到列表中。

    参数:
        - 列表 (list): 要插入元素的列表。
        - 位置 (int): 插入元素的位置索引。
        - 元素: 要插入的元素。

    返回:
        - list: 插入元素后的新列表。如果出现异常，返回原始列表。
    """
    try:
        # 使用列表的 insert 方法在指定位置插入元素
        列表.insert(位置, 元素)
        return 列表
    except Exception:
        return 列表