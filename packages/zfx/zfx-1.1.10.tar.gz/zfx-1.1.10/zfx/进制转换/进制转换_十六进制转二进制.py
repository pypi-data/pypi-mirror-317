def 进制转换_十六进制转二进制(十六进制数):
    """
    将给定的十六进制数转换为二进制表示形式。

    参数:
    十六进制数 (str): 要转换的十六进制数，不包括前缀 '0x'

    返回:
    str: 十六进制数的二进制表示形式，不包括前缀 '0b'；如果转换失败或输入不符合要求，则返回 None

    示例:
        转换结果 = 进制转换_十六进制转二进制('1A')
        print(转换结果)
    """
    try:
        # 将十六进制数转换为整数
        十进制数 = int(十六进制数, 16)
        # 使用内置函数bin()进行十进制转二进制转换
        return bin(十进制数).replace("0b", "")
    except:
        return None

