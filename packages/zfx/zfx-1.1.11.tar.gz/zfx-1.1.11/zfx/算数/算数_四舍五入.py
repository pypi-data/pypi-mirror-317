def 算数_四舍五入(数字, 小数位数=0):
    """
    返回对输入数字进行四舍五入后的结果。

    参数：
    数字 (数值型): 需要四舍五入的数字。
    小数位数 (整数型, 可选): 四舍五入后保留的小数位数，默认为0。

    使用示例：
    print(算数_四舍五入(3.14159))  # 输出：3
    print(算数_四舍五入(3.14159, 2))  # 输出：3.14

    返回：
    数值型: 四舍五入后的结果。如果出现异常则返回 False。
    """
    try:
        return round(数字, 小数位数)
    except Exception:
        return False