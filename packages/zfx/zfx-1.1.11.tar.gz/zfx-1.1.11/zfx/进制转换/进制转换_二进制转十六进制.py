def 进制转换_二进制转十六进制(二进制字符串):
    """
    将二进制字符串转换为十六进制字符串。

    参数:
        二进制字符串 (str): 要转换的二进制字符串。

    返回:
        str: 转换后的十六进制字符串，如果输入不是有效的二进制字符串则返回 None。

    示例使用:
        十六进制字符串 = 进制转换_二进制转十六进制('110010101011')
        print("转换后的十六进制字符串:", 十六进制字符串)
    """
    try:
        十六进制字符串 = hex(int(二进制字符串, 2))[2:]  # 去掉前缀'0x'
        return 十六进制字符串
    except Exception:
        return None