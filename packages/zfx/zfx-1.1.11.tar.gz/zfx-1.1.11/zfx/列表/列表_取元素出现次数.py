def 列表_取元素出现次数(列表, 目标元素):
    """
    统计目标元素在列表中出现的次数。

    参数:
        - 列表 (list): 包含元素的列表
        - 目标元素: 要统计出现次数的目标元素

    返回:
        - 出现次数 (int): 目标元素在列表中出现的次数，如果出现异常则返回错误信息。
    """
    try:
        出现次数 = 0

        # 遍历列表中的每个元素
        for 元素 in 列表:
            # 如果元素等于目标元素，则计数加一
            if 元素 == 目标元素:
                出现次数 += 1

        return 出现次数
    except Exception as e:
        # 捕获所有异常并返回错误信息
        return f"处理失败: {e}"